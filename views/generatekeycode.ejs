<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <%- include('./config/import') %>
</head>
<body class="bg-white">

    <%- include('./components/nav') %>
    <div class="flex justify-center px-5">
        <div class="w-full max-w-7xl">
            <%- include('./components/navadmin') %>           
            <section>
                <div class="max-w-3xl px-6 py-16 mx-auto text-center">
                    <h1 class="text-3xl font-semibold text-gray-800">Generate Key Code</h1>
                    <p class="max-w-md mx-auto mt-5 text-gray-500">Lorem ipsum dolor sit amet, consectetur adipisicing elit. Perferendis, minus tempora nemo quos</p>
                    <% if (message) { %>
                        <div class="flex justify-center my-5">
                            <div class="w-full max-w-[400px] py-1 border rounded-xl flex items-center gap-2 justify-center">
                                <div>
                                    <p>
                                        <%= message %>
                                    </p>
                                </div>
                                <div class="mt-[7px]">
                                    <button class="copycode" onclick="copyCode('<%= message %>')">
                                        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-copy"><rect width="14" height="14" x="8" y="8" rx="2" ry="2"/><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/></svg>
                                    </button>
                                </div>
                            </div>
                        </div>
                    <% } %>
                    <form id="generateCodeForm" action="/api/codes/generatecode" method="POST">
                        <div class="flex flex-col mt-8 space-y-3 sm:space-y-0 sm:flex-row sm:justify-center sm:-mx-2">
                            <input id="points" name="points" type="number" class="px-4 py-2 text-gray-700 bg-white border rounded-xl sm:mx-2 focus:border-[<%= config.color %>] focus:outline-none focus:ring focus:ring-[<%= config.color %>] focus:ring-opacity-40" placeholder="Enter the amount">
                
                            <button type="submit" class="flex gap-2 items-center px-4 py-2 text-sm font-medium tracking-wide text-white capitalize transition-colors duration-300 transform bg-[<%= config.color %>] rounded-xl sm:mx-2 hover:bg-[<%= config.color %>] focus:outline-none focus:bg-[<%= config.color %>]">
                                <span>
                                    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-code-xml"><path d="m18 16 4-4-4-4"/><path d="m6 8-4 4 4 4"/><path d="m14.5 4-5 16"/></svg>
                                </span>
                                Generate Key Code
                            </button>
                        </div>
                    </form>
                </div>
            </section>
        </div>
    </div>
    <%- include('./components/footer') %>
    <script>
        function copyCode(code) {
            navigator.clipboard.writeText(code).then(() => {
                alert('Code copied to clipboard!');
            }).catch(err => {
                console.error('Failed to copy code: ', err);
                alert('Failed to copy code.');
            });
        }
    </script>

</body>
</html>