<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <%- include('./config/import') %>
</head>
<body class="bg-zinc-50">

    <%- include('./components/nav') %>
    <div class="flex justify-center px-5">
        <div class="w-full max-w-7xl">
            <section>
                <div class="max-w-3xl px-6 py-16 mx-auto text-center">
                    <h1 class="text-3xl font-semibold text-gray-800">Top up via code</h1>
                    <p class="max-w-md mx-auto mt-5 text-gray-500">Lorem ipsum dolor sit amet, consectetur adipisicing elit. Perferendis, minus tempora nemo quos</p>
                    <% if (message) { %>
                        <div class="my-[20px]">
                            <%= message %>
                        </div>
                    <% } %>
                    <form action="/api/codes/fillcode" method="POST">
                        <div class="flex flex-col mt-8 space-y-3 sm:space-y-0 sm:flex-row sm:justify-center sm:-mx-2">
                            <input id="code" name="code" type="text" class="px-4 py-2 text-gray-700 bg-white border rounded-xl sm:mx-2 focus:border-amber-400 focus:outline-none focus:ring focus:ring-amber-300 focus:ring-opacity-40" placeholder="Enter top-up code">
                
                            <button type="submit" class="flex items-center gap-1 px-4 py-2 text-sm font-medium tracking-wide text-white capitalize transition-colors duration-300 transform bg-amber-600 rounded-xl sm:mx-2 hover:bg-[<%= config.color %>] focus:outline-none focus:bg-amber-600">
                                <span>
                                    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-plus"><path d="M5 12h14"/><path d="M12 5v14"/></svg>
                                </span>
                                Top up
                            </button>
                        </div>
                    </form>
                </div>
            </section>
        </div>
    </div>
    
</body>
</html>